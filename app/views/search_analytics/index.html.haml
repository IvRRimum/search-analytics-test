.row
  .col-lg-9
    %h1="Search Analytics"

    %hr
    - @search_analytics.each do |search_analytic|
      %span
        = "#{search_analytic.search_term} (#{search_analytic.search_count})"
      %hr

  .col-lg-3
    %h3="Actions"
    = link_to "Clear Search Analytics", delete_all_search_analytics_path, 
      method: :delete, data: { confirm: "You sure?" }, class: "btn btn-danger btn-block"
